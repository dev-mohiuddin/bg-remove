# Place BiRefNet and ViTMatte ONNX model files here.
#
# Run `python download_models.py` from the backend/ directory to
# automatically download or export the models.
#
# Expected files:
#   birefnet.onnx   — BiRefNet segmentation model (SOTA)
#   vitmatte.onnx   — ViTMatte alpha matting model (optional)